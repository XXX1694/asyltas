import 'package:flutter/material.dart';

class CartItemMock extends StatelessWidget {
  const CartItemMock({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Image.asset(''),
      ],
    );
  }
}
